# Plan de limpieza de ramas

> No borrar `main`.

## Orden recomendado

1. **Merge** `feat/ui-studio-official` → `main`
2. **Borrar** rama `feat/ui-studio-official` tras merge
3. Revisar y borrar ramas obsoletas tras verificar que no contienen trabajo pendiente

## Ramas a considerar borrar (tras merge)

- `feat/ui-studio-official` — tras merge a main
