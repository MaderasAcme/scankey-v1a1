/**
 * Temporal web testing login; replace with backend auth when gateway auth is restored.
 * Cuando TEMP_SIMPLE_WEB_LOGIN es true, el login valida credenciales en frontend
 * sin llamar a /api/auth/login.
 */
export const TEMP_SIMPLE_WEB_LOGIN = true;
export const TEMP_LOGIN_EMAIL = 'scankey@scankey.com';
export const TEMP_LOGIN_PASSWORD = '1357';
